apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mapproxy-cache
spec:
  storageClassName: managed-csi-retain
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
